@using BTL.Models
@model List<HangSX>

    <div class="col-md-3 animate-box" data-animate-effect="fadeInRight">
        <div>
            <div class="fh5co_heading fh5co_heading_border_bottom py-2 mb-4"><p>Hãng Sản Xuất</p></div>
        </div>
        <div class="clearfix"></div>
        <div class="fh5co_tags_all">
            @{
                int i = 0;
                foreach (var item in Model)
                {
                    <a href="@Url.Action("sanPhamTheoHang", "Home", new { @MaHSX = item.MaHSX})" class="fh5co_tagg">@item.TenHSX</a>
                }
            }
        </div>
        @Html.Action("topSale", "Home")
    </div>
